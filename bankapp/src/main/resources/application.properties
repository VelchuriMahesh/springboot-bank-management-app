# ✅ Server Port Configuration
server.port=8081

# ✅ Application Name
spring.application.name=bankapp

# ✅ MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bankappdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Pravallika@2004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ✅ JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Optional: Recommended for REST API (disables lazy loading in views)
spring.jpa.open-in-view=false
